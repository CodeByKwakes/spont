<div class="container">
  <div class="page-header">
    <h1>{{user.firstName}} {{user.lastName}}</h1>
  </div>

  <div class="row">
    <div class="col-sm-12">
      <div class="panel panel-default text-left">
        <div class="panel-body">
          <p contenteditable="true">{{user.gender}}</p>
          <p contenteditable="true">{{user.email}}</p>
          <p contenteditable="true">{{user.postcode}}</p>
        </button>
      </div>
    </div>
  </div>
</div>

<br><hr><br>

<div class="row">
  <div class="col-s4 center" >
    <a href="/users" class="center btn">Back</a>
  </div>
  <div class="col-s4 center" >
    <a href="/users/{{user.id}}/edit"class="center btn">Edit</a>
  </div>
  <div class="col-s4 center">
    <form method="post" action="/users/{{user.id}}">
      <input type="hidden" name="_method" value="delete">
      <button class="center btn">Delete</button>
    </form>
  </div>
</div>
</div>    